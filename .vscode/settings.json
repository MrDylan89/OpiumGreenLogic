{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "users",
            "database": "${workspaceFolder:terranova_Project}/database/autenticazione.db"
        }
    ]
}